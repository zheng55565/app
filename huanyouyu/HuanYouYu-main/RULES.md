# 技术规范（RULES.md）
本文件定义项目的长期技术规范。除非在任务中被明确豁免，否则所有实现都必须遵循本文件。

## 1) UI 文本与字体
- 新增 UI 文本必须使用 TMP（`TextMeshProUGUI` / `TextMeshPro`），禁止新增 `UnityEngine.UI.Text`。
- 子游戏 UI 文案必须独立维护在文案配置文件中，禁止在脚本内直接写死用户可见文本。

## 2) 输入系统
- `EventSystem` 必须使用 `StandaloneInputModule`。

## 3) 可复用与结构
- 需要重复实例化或跨场景复用的对象（UI/非 UI）必须 prefab 化或模块化。
- 优先复用已有模块与模板，避免一次性脚本堆叠。

## 4) 实现复杂度与抽象控制
- 默认使用最简单、最直接、最容易理解的实现方式，避免过度设计。
- 未经明确要求，不要新增接口、抽象基类、工厂、反射、注册表或其他额外抽象层。
- 优先在现有结构上做最小改动，不要为了局部需求重构整体架构。
- 如果存在多个可行方案，优先选择文件更少、层级更少、调用链更短的方案。
